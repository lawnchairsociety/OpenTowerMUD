# Chat Filter Test Configuration
enabled: true
mode: REPLACE
banned_words:
  - badword
  - forbidden

# Anti-spam Configuration
antispam:
  enabled: true
  max_messages: 5
  time_window_seconds: 10
  repeat_cooldown_seconds: 30
